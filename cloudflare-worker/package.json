{
  "name": "fm-global-rag-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "esbuild worker.ts --bundle --format=esm --outfile=dist/worker.js",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 execute fm-global-rag --file=schema.sql",
    "secret": "wrangler secret put OPENAI_API_KEY"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "esbuild": "^0.20.0",
    "wrangler": "^3.0.0"
  }
}